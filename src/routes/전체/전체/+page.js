/*
export async function load(event) {
    const response = await event.fetch(
        '/api/examinees?' +
            new URLSearchParams({
                major: 'ㅋㅋㅋㅋㅋㅋㅋㅋㅋ',
            }).toString(),
        {
            method: 'GET',
        },
    )
    const items = await response.json()
    return {
        items,
    }
}
*/
